# Pipelines


This directory contains all of the pipelines that are used in the simulator. These pipelines are built using the processing elements in the ASA directory.

- `seizure`
    - Contains seizure detection pipelines
