# IEEG Datasets

https://ieeg.org

```
cd ieegpy
python setup.py build
python setup.py install
```

Run script:

```
python get_data.py -u ${USER} -p ${PWD} ${DATASET} ${START} ${DURATION}
```

## Dataset Names

* `I001_P013_DXX`
    * Up to 72 channels, replace XX
