# ASA

This is the accelerator set architecture (ASA) directory. This directory
contains all of the processing elements that are used in pipelines.

To run a file in this directory, use the notation below:

`pytest test_element.py -v -rP`


### To do

hardware `def run_hdl()` functions somewhere for each PE, use dimension_validate to check dimensions

- implement hardware functions
- Auto pass/fail test cases on github

- how to define a pipeline and run it
- how to create new processing elements
- the window class
- parent classes
